"use strict"; // use this keyword treat all Js code as a newer version (optinal)

//alert(4+5) // we are using NodeJs, not Browser

console.log(3
    + 4
) //its is not a good practice, code readablility should be high

let name = "Arrow" // String
let age = 23       // number
let isValid = false // boolean
let percantage = 3.4 // its is also a number not like other language like float 
// null  => standalon value
// symbol => unique

console.table([typeof age,
              typeof name, 
              typeof isValid, 
              typeof percantage, 
              typeof undefined, 
              typeof null
            ])